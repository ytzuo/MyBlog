{
  "name": "",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "check": "astro check",
    "check:content": "node scripts/check-content.mjs",
    "check:links": "node scripts/check-markdown-links.mjs",
    "test": "vitest run",
    "test:dom": "vitest run tests/MermaidEnhancements.spec.ts",
    "test:content": "vitest run tests/content-metadata.spec.ts",
    "test:coverage": "vitest run --coverage",
    "build": "astro build",
    "quality": "pnpm run check && pnpm run check:content && pnpm run test && pnpm run build && pnpm run check:links",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/mdx": "^6.0.0",
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^7.0.3",
    "mermaid": "^11.13.0",
    "sharp": "^0.34.3"
  },
  "packageManager": "pnpm@11.1.0",
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@vitest/coverage-v8": "^4.1.10",
    "jsdom": "^30.0.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
